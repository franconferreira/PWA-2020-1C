// Metasploit framework

//  Visual Studio Code ( Typescript )
// GIT 
// XAMPP | WAMP | LAMP | MAMP 
// AWS | Heroku  
// Node (npm -> angular | react | nodemon | etc)
// Jira | Trello
// PuTTy
// Winscp
// POSTMAN 



// PATRONES DE DISEÑO (MVC) -> MODELO VISTA Y CONTROLADOR
// PATRONES CREACIONALES (SINGLETON, BUILDER)



// 1. Javascript (proto, POO)
// 2. POO | MySQL (xampp)

    // API ->  Integraciones con MP | Spootify
    // Aplication program interfaz
    // Capa de abstracción
    // REST (Representational state transfer) : headers y peticiones (POST, DELETE, CREATE, UPDATE)

// 3. NodeJS (express )
// ...
// CRUD (MYSQL | MongoDB (no relacionales))
// JWT 
// 8 node

// Framework : Frontend (Muchas librerias)
// 9 Angular +9 (POO)
    // Modales
    // SessionStorage | LocalStorage
    // Observers
    // DOM
// 16 Angular +9

// 17 a 21
// React (libreria --> )

// 22 y 23 (servidores)

// Subido a un servidor web (AWS || Heroku)
// Proyecto (Backend (Node) + Frontend (React || React) + Modelo de datos (MySQL || Mongo))

// 6 funcionalidades (auth, login, registro, envio)
// 1 integración o + (MP, Login con facebook, Twitter)
// Angular | React 
